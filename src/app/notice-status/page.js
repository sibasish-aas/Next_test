import  NoticeList from '../../components/notice/ApprovalStatus';
import Layout from '../../components/layout';

const  ApprovalStatusPage = () => {
  return (
    <Layout>
        <div>
            < NoticeList />
        </div>
    </Layout>
  );
};

export default  ApprovalStatusPage;